import 'package:flutter/material.dart';

class WlbPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('WLB'),
        backgroundColor: Colors.amber[300],
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
