import 'package:flutter/material.dart';

class FrePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FRE'),
        backgroundColor: Colors.deepPurple[800],
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
