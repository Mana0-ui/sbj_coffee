import 'package:flutter/material.dart';

class EdbPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EDB'),
        backgroundColor: Colors.orange,
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
