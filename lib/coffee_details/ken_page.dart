import 'package:flutter/material.dart';

class KenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('KEN'), backgroundColor: Colors.orange),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
