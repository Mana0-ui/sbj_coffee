import 'package:flutter/material.dart';

class LnbPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LNB'),
        backgroundColor: Colors.amber[300],
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
