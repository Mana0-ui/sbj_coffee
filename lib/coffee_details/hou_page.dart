import 'package:flutter/material.dart';

class HouPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('HOU'),
        backgroundColor: Colors.orange,
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
