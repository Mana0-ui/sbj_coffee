import 'package:flutter/material.dart';

class GuaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('GUA'),
        backgroundColor: Colors.orange,
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
