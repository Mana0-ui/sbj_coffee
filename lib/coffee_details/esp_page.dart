import 'package:flutter/material.dart';

class EspPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ESP'),
        backgroundColor: Colors.deepPurple[800],
      ),
      body: Container(
        height: double.infinity,
      ),
    );
  }
}
